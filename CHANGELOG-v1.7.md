# 大富翁之金聪明游浙江 v1.7.0

本次优先修复无法加入联机房间和 AI 托管异常，保留现有地图、八级建筑、卡牌及经济数值。前端仍部署在 GitHub Pages，后端继续使用原腾讯云 CloudBase 环境。

## 联机修复

- 实际云端复现：按文档 ID 读取房间时，当前 SDK 将房间的 `code` 字段作为错误码抛出；改为按房间号进行列表查询，并核验文档 ID 和玩家列表。
- 实际云端返回 `presence` 集合不存在。缺少该集合时，使用 `rooms` 中带 `kind: presence` 标记和独立 ID 的心跳记录，不更改数据库权限。
- 云端认证和读取检查完成后才允许进房；创建或加入成功写入云端后才展示房间。
- 连接失败显示原因及重试入口，取消隐式本地房间，避免两台设备各自进入互不相通的游戏。
- 更新房间时使用房间 ID 和已读取的 `seq` 做条件更新；旧客户端快照写入被拒绝后重新读取当前状态，并提示重试操作。
- 读取请求返回时检查当前房间，防止退出或切换房间后旧响应覆盖新界面。

## 托管和规则修复

- AI 驱动客户端只从真人中选择；电脑玩家不能成为接管客户端，在线且已破产的房主仍可驱动电脑回合。
- 缺失心跳超过宽限期后视为离线；在线状态查询过期时暂停驱动选举，降低错误接管风险。
- 暂停和监狱回合重新设置计时，避免沿用上一回合时间而立即托管。
- 取消托管重置自己的计时；其他玩家切换托管不再修改当前玩家倒计时。
- 自动结算绑定原房间和原事件，不能误处理后续事件或取消刚开启的托管。
- 托管行动间隔至少 1.6 秒，写入未完成时等待，给骰子动画和云端同步留出时间。
- 买地和建设保留至少 ¥1000 或半次当前起点奖励作为周转资金；机会按收益风险判断；紧急支付先抵押卡牌再出售地产。
- 引擎拒绝重复掷骰、掷骰后主动使用卡牌或抵押；欠费时各种结束回合入口都不能逃避结算。

## 页面和内容

- 连接状态、失败重试、键盘焦点与房间号输入提示。
- 手机顶部信息自动换行，避免回合信息挤成竖排；棋盘增加横向滑动提示。
- 头像区域收纳为可滚动列表，新手可展开一分钟玩法说明。
- 修正“10 秒自动跳过”“结算自动进入下一人”等与实现不符的说明。
- 页面及资源版本号更新为 1.7.0。

## 验证

- 10 项自动回归通过：暂停计时、欠费保护、掷骰后的操作限制、紧急抵押精确结算、登录失败和重试、双浏览器入房、旧快照写入拦截、AI 接管人选举、浏览器无未捕获异常。模拟环境同时覆盖缺失 presence 集合。
- 使用真实 CloudBase 和两个独立浏览器会话验证：登录、创建房间、加入房间、双方同步、托管推进行动、取消托管同步。测试页面通过浏览器请求拦截加载候选源码，并未提前替换线上文件。
- 查看 1440 像素桌面和 390 像素手机截图，修复手机顶部布局。棋盘仍采用横向滑动方式，未做地图重绘。
- 专用测试房间及相关心跳记录已清理。

## 发布和已知边界

一起发布 app.js、engine.js、styles.css 和 index.html；data.js 不变。上线后所有玩家应刷新到 v1.7.0 再新建房间，不建议新旧客户端混玩。

这不是完整的服务端权威规则重构。客户端仍能读取整个房间状态，特殊卡牌隐私仅是界面隐藏；高延迟、浏览器长期后台挂起、多设备使用同一身份等情形仍需后续专门验证。短时双客户端测试不能代表所有手机和网络条件。

交接文档中的明文访问令牌没有用于本次发布，也没有复制到修复包；应在 GitHub 撤销并更换。

CloudBase 接口参考：[数据库条件更新](https://docs.cloudbase.net/api-reference/webv2/database/update)、[匿名登录](https://docs.cloudbase.net/authentication-v2/method/anonymous)。
